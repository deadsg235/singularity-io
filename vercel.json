{
    "buildCommand": "cd api && pip install -r requirements.txt",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index" },
        { "source": "/(.*)", "destination": "/web/singularity-frontend/$1" }
    ]
}
