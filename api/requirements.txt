# Singularity.io API — Vercel serverless (Python 3.12)
# Keep this lean — Vercel Lambda limit is 245 MB uncompressed.
# torch, solana-py, anchorpy are NOT listed here because they exceed the limit.
# The DQN engine and Solana RPC calls use lazy imports with graceful fallbacks.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
mangum==0.17.0

# Async HTTP (Guardian RPC calls, Jupiter API, Groq)
httpx==0.27.2

# Data validation
pydantic==2.9.2

# AI / LLM (Groq client is small — ~2 MB)
groq==0.11.0

# Utilities
python-dotenv==1.0.1
